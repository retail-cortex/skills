[project]
name = "example-adk"
version = "1.0.0"
description = "Native Google ADK Agent Example loading local and remote GitHub skills"
requires-python = ">=3.13"
dependencies = [
    "google-adk>=1.16.0",
    "google-genai>=1.0.0",
    "tenacity>=9.0.0",
    "skills-loader",
]

[tool.uv]
package = false
