[project]
name = "skills-loader"
version = "1.0.0"
description = "Reusable enterprise AI agent skill scanner and loader for Google ADK"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
