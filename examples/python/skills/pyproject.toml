[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "retailcortex-skills-python"
version = "1.0.0"
description = "Retail Cortex Python 3.13, FastAPI, ADK & FastMCP AI agent skill definitions"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.13"
dependencies = []
authors = [
    { name = "Retail Cortex Engineering" }
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
]

[project.urls]
Homepage = "https://github.com/retail-cortex/castor"
Repository = "https://github.com/retail-cortex/castor"

[project.entry-points."retailcortex.castor"]
python = "retailcortex_skills_python"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["*.*", "**/*", "**/.*"]

[tool.uv]
package = true
