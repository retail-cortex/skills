# Default configuration for skills-service loaded via modenv
host = "0.0.0.0"
port = 8000
database_url = "skills.db"
enable_opentelemetry = false
otel_service_name = "skills-service"
gcp_project_id = ""
embedding_provider = "vertex-gemini"
