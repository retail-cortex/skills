load("@rules_go//go:def.bzl", "go_library", "go_test")

package(default_visibility = ["//visibility:public"])

go_library(
    name = "commands",
    srcs = ["commands.go"],
    importpath = "github.com/retail-cortex/skills/cli/internal/commands",
    deps = [
        "//cli/internal/installer",
        "//cli/internal/validator",
        "//clients/go/pkg/skillsloader",
    ],
)

go_test(
    name = "commands_test",
    srcs = ["commands_test.go"],
    embed = [":commands"],
    deps = [
        "@com_github_stretchr_testify//assert",
    ],
)
